[pytest]
# pytest-embedded configuration for ESP-IDF QEMU tests

markers =
    esp32: ESP32 target tests
    esp32s3: ESP32-S3 target tests
    qemu: Tests that run in QEMU
    generic: Generic environment (used for QEMU/simulation)

# Show extra test info
addopts = -v
